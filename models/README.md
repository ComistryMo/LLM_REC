# Models

Do not commit model weights.

Default base model path on the remote machine:

```text
/data/hz/models/OneReason-0.8B-pretrain-competition
```

Download with:

```bash
bash scripts/setup/download_model.sh
```

