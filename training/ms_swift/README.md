# ms-swift training

Use `scripts/train/train_swift_lora.sh` for the first baseline and `scripts/train/train_swift_full.sh` for full-parameter SFT.

Run from the project root after activating `onereason-rec`:

```bash
bash scripts/train/train_swift_lora.sh
```

